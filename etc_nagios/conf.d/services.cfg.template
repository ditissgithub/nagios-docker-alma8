define service{
        use                     generic-service
        hostgroup_name          compute, rbhmem, rbgpu, master, login
        service_description     Current Users
        check_command           check_nrpe!check_users
        }

define service{
        use                     generic-service
        hostgroup_name          compute, rbhmem, rbgpu, master, login, management
        service_description     Zombie Processes
        check_command           check_nrpe!check_zombie_procs
        }

define service{
        use                     generic-service
        hostgroup_name          compute, rbhmem, rbgpu, master, login, management
        service_description     Node Load
        check_command           check_nrpe!check_load
        }

define service{
        use                     generic-service
        hostgroup_name          compute, rbhmem, rbgpu, master, login
        service_description     Check IB
        check_command           check_nrpe!check_ib
        }

define service{
        use                     generic-service
        hostgroup_name          master, management
        service_description     Httpd Status
        check_command           check_nrpe!check_httpd
        }

define service{
        use                     generic-service
        hostgroup_name          master, compute
        service_description     Slurm Status
        check_command           check_nrpe!check_slurmd
        }

define service{
        use                     generic-service
        hostgroup_name          compute, rbhmem, rbgpu, master, login, management
        service_description     Check NTP
        check_command           check_nrpe!check_ntp
        }
